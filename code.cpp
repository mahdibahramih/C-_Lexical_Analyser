#include "stdafx.h"
#include < iostream > std;
using namespace std;
void main ()
    {
    int a , b , c , max , min ;
    cout << "adadravaredkonid:" << endl;
    cin >> a ;
    cin >> b ;
    cin >> c ;
    max = a ;
    min = a ;
    if ( b > max )
        max = b ;
    if ( b < min )
        {
            min = b ;
        }
    if( c > max )
    max = c ;
    if ( c < min )
        min = c ;
    cout << "maxis:" << max<< endl ;
    cout << "minis:" << min ;
    cin.get();
    a = 2222
    b = 22.22
    cin.ignore();
/*a*/
}
